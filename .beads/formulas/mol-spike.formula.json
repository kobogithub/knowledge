{
  "formula": "mol-spike",
  "version": 1,
  "description": "Template for time-boxed technical investigation (spike)",
  "variables": {
    "name": {
      "description": "Spike name (kebab-case, e.g. 'evaluate-vector-db')",
      "required": true
    },
    "description": {
      "description": "What question does this spike answer?",
      "required": true
    },
    "timebox": {
      "description": "Time limit for the spike (e.g. '4h', '1d')",
      "default": "4h"
    },
    "assignee": {
      "description": "Agent responsible for the investigation",
      "required": true
    }
  },
  "steps": [
    {
      "id": "research",
      "title": "Spike: Investigar {{name}}",
      "type": "task",
      "priority": 1,
      "labels": ["phase:exploration", "spike", "{{name}}"],
      "assignee": "{{assignee}}",
      "description": "Investigacion time-boxed ({{timebox}}): {{description}}. Documentar hallazgos, prototipos y recomendacion."
    },
    {
      "id": "adr",
      "title": "ADR: Decision sobre {{name}}",
      "type": "decision",
      "priority": 1,
      "labels": ["adr", "spike", "{{name}}"],
      "assignee": "{{assignee}}",
      "depends_on": ["research"],
      "description": "Documentar la decision resultante del spike {{name}}. Incluir: contexto, alternativas evaluadas, decision, consecuencias."
    },
    {
      "id": "recommendation",
      "title": "Planner: Evaluar resultado de spike {{name}}",
      "type": "task",
      "priority": 1,
      "labels": ["phase:specification", "planner", "{{name}}"],
      "assignee": "knowledge-x6e",
      "depends_on": ["adr"],
      "description": "Evaluar el ADR del spike y decidir proximos pasos: crear epic de implementacion, descartar, o profundizar."
    }
  ]
}
